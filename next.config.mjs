/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn-icons-png.flaticon.com', 'static.mirats.cloud', 'static.miratsapiservices.com'],
      }
};

export default nextConfig;
